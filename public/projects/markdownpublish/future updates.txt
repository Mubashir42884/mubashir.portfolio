1. Make the fonts a input box where user can write their own fonts available on device.
2. The Content "Image" adding should give user options to select of uploading from device, or add from URL. For URL images, add an HTML <img> tag container with center alignment, 50% width. For uploading from device, download the selected image locally on a cache path and load it from there as same <img> tag. Because uploading from local gives it a massive line of image codes and very annoying for users.
3. Give all the options to manually set alignments, line gaps, character gaps, and settings for custom Heading (H1, H2, H3, H4, H5) character size and save it as a custom style name for later use. Also give options to customize colors, line gaps, and fonts for headlines, paragraph, quotation, and codes. Add a tool icon button in the Heading that gives these options to the user.
4. Add a burger icon button at the leftmost corner before Format buttons to add a sidebar. It will have the below options in it:
	i. Styles: it will include all the custom style lists the user has created. Will have the option to edit or delete it in a 3 dot button beside the styles. 
	ii. Files: includes all the history list of markdown files the user has opened by remembering the local path they used to open the file. Give the 3 dot button on side of each file to export as HTML/md/pdf, or remove from list. If the file is not opened using the filepath, show an error saying "The file is deleted/moved/renamed from the local path."
	iii. Settings: add it at the bottom before the copyright claim of the webapp that will have all the necesarry settings for the webapp that an user should able to change.
5. Add a next line or line whitespace button with cutom gap space in Extra buttons. So that I can add some extra custom space between the next line.
6. Add the single line code block in the format that makes selected text like this: `selected text` or default `text`. Name it "</>" and add after the ">" format button.
7. I want the "Save .md" button to work as Save as .md that gives the user option to download the file wherever they want in their storage. Same thing wuth the "Export HTML".
